# Copy to .env for local development. In the homelab, Ansible injects these.
# Nothing here has a secret as a default — the service refuses to start without
# DEVICE_TOKEN and DARWIN_ACCESS_TOKEN.

# Shared bearer the Device presents. Also accepted as ?token= so you can open
# /preview in a browser.
DEVICE_TOKEN=change-me

# Darwin OpenLDBWS token, passed through to Huxley2.
# Note: National Rail retired self-service token issuing in early 2026 — a
# replacement means registering at raildata.org.uk. Do not lose this one.
DARWIN_ACCESS_TOKEN=change-me

# The public Huxley2 instance has no uptime guarantee. Point this at a
# self-hosted one when that starts to bite.
HUXLEY_BASE_URL=https://huxley2.azurewebsites.net

STATION_ORIGIN=KEL
STATION_DESTINATION=LST

# How far ahead a departure must be to be worth showing. Deliberately shorter
# than the ten minute walk to Kelvedon: a train you would have to run for is
# still worth knowing about, and you can judge that better than a rule can.
DEPARTURE_MIN_LEAD_MINUTES=5

# --- Met Office (optional) ---
# DataHub account with a Site Specific "Global Spot" subscription. Free tier is
# 360 calls/day; this uses about 90. Without it the weather zone says so and
# every other zone is unaffected.
MET_ACCESS_TOKEN=
MET_BASE_URL=https://data.hub.api.metoffice.gov.uk

# Where the display lives. Defaults to Kelvedon.
HOME_LATITUDE=51.8382
HOME_LONGITUDE=0.7018

# --- Family Hub (optional) ---
# Token from POST /api/tokens as an admin. Note there is no read-only scope:
# this token can write. It never leaves the server, which is the mitigation.
# Both must be set or the household zones report themselves unconfigured.
HUB_ACCESS_TOKEN=
HUB_BASE_URL=

TZ=Europe/London

# Port inside the container. Leave it alone unless you have a reason.
PORT=8080

# Port published on the host. This is the one the device's FRAME_URL must use.
DISPLAY_HOST_PORT=8080
